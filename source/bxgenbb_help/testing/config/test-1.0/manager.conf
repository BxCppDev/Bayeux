# -*- mode: conf-unix; -*-

#@config Main configuration parameters for the genbb manager

#@description Debug flag
debug : boolean = 0

#@description Seed for the embeded PRNG (used only if no external PRNG is provided)
seed : integer = 314159

#@description List of files containing particle generator definition rules
generators.configuration_files : string[4] as path = \
  "${GENBB_HELP_TESTING_DIR}/config/test-1.0/backgrounds.conf"  \
  "${GENBB_HELP_TESTING_DIR}/config/test-1.0/calibrations.conf" \
  "${GENBB_HELP_TESTING_DIR}/config/test-1.0/dbd.conf"          \
  "${GENBB_HELP_TESTING_DIR}/config/test-1.0/misc.conf"


# end
