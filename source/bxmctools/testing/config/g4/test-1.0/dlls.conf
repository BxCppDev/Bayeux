# List of multi-properties (datatools::multi_properties):

#@description A sample list of setups
#@key_label   "name"
#@meta_label  "filename"

############################################################
[name="boost_thread" filename=""]
#@config The libboost_thread library

#@description The full path of the shared library file
autoload  : boolean = 1

############################################################
[name="mctools_g4" filename=""]
#@config The libmctools_g4 library

#@description The full path of the shared library file
#full_path : string = "${MCTOOLS_LIB_DIR}/libmctools_g4.so"
autoload  : boolean = 1

# End of list of multi-properties.
